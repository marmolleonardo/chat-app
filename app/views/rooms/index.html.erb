<%= turbo_frame_tag 'rooms_controller' do %>
  <main class="flex w-full h-screen bg-blue-950 text-white">
    <%= render 'shared/side_bar' %>

    <section class="flex flex-col items-center justify-center gap-5 h-screen w-full">
      <%= image_tag '5041093.png', width: 200 %>
      <h1 class="text-blue-900 dark:text-white text-4xl font-bold mb-2">ChatApp</h1>
      <p class="text-gray-700 dark:text-white font-semibold">This is the chat app with RoR using Stimulus</p>
    </section>
  </main>
<% end %>
